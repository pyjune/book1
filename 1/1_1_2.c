#include <stdio.h>

int main(void)
{
	int T;
	freopen("input.txt", "r", stdin);  // 표준입력을 파일로 변경됨
	scanf("%d", &T); // 테스트케이스 개수 T를 먼저 읽고
	for (int tc = 1; tc <= T; tc++)  // T만큼 반복
	{
		// 이 부분에 N개의 정수를 읽고 최대값을 찾는 코드가 들어간다.
	}
}
